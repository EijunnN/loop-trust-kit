node_modules
.DS_Store
*.log

# Local, per-machine budget ledger — never committed.
.loop/budget.json

# Local plugin enable/disable state (written by `/plugin install`); not shipped.
.claude/settings.json
.claude/settings.local.json
